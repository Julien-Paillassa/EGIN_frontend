export interface Board {
    name: string;
    brand: string,
    size: string,
    volume: string,
    description: string,
    dimension: string
}

export interface BoardInfo {
    name: string;
    id: string;
    createdAt: string;
}
  